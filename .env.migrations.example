# Local-only helper for running Supabase migrations.
# Copy to `.env.migrations` and fill in secrets.
#
# IMPORTANT: don’t put DB passwords into `.env` / `.env.local` (Next loads those).

SUPABASE_PROJECT_REF=

# Option A: DB password (used with `supabase db push --password ...`)
SUPABASE_DB_PASSWORD=

# Option B: full connection string (alternative; must be percent-encoded for Supabase CLI `--db-url`)
# Example (non-encoded): postgresql://postgres:<PASSWORD>@db.<PROJECT_REF>.supabase.co:5432/postgres
SUPABASE_DB_URL=

